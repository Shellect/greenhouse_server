# 🍓 Greenhouse Provisioning App

Android приложение для настройки контроллеров теплицы NodeMCU.

## Функциональность

- 📱 **Сканирование QR кода** - распознавание QR кода на корпусе устройства
- 📶 **WiFi Provisioning** - автоматическое подключение к AP устройства
- ⚙️ **Wizard настройки** - пошаговая настройка WiFi и сервера
- ✅ **Обработка ошибок** - повторные попытки при сбоях подключения

## Требования

- Android 8.0 (API 26) или выше
- Камера для сканирования QR
- WiFi модуль
- Разрешения: Camera, Location (для WiFi)

## Сборка проекта

### Требования

- Android Studio Hedgehog (2023.1.1) или новее
- JDK 17
- Gradle 8.2+

### Шаги

1. Откройте проект в Android Studio:
   ```bash
   cd android-app
   # Откройте эту папку в Android Studio
   ```

2. Синхронизируйте Gradle:
   - Android Studio автоматически предложит синхронизацию
   - Или: `File → Sync Project with Gradle Files`

3. Соберите APK:
   ```bash
   ./gradlew assembleDebug
   ```
   
   APK будет в `app/build/outputs/apk/debug/app-debug.apk`

4. Для release сборки:
   ```bash
   ./gradlew assembleRelease
   ```

## Использование

### Шаг 1: Сканирование QR кода

1. Запустите приложение
2. Нажмите "Scan Device QR Code"
3. Наведите камеру на QR код устройства

### Шаг 2: Подключение к устройству

Приложение автоматически:
1. Подключится к WiFi сети устройства (AP mode)
2. Проверит доступность устройства
3. Получит список WiFi сетей

### Шаг 3: Настройка WiFi

1. Выберите вашу домашнюю WiFi сеть
2. Введите пароль WiFi
3. Укажите адрес сервера теплицы
4. Нажмите "Save Configuration"

### Готово!

Устройство перезагрузится и подключится к вашей WiFi сети.

## Архитектура

```
com.greenhouse.provisioning/
├── MainActivity.kt           # Главный экран
├── QRScannerActivity.kt      # Сканер QR кодов
├── WifiSetupActivity.kt      # Подключение к AP устройства
├── ConfigurationActivity.kt  # Настройка WiFi и сервера
├── SetupCompleteActivity.kt  # Экран успешного завершения
├── DeviceInfo.kt             # Модели данных
├── DeviceApiService.kt       # Retrofit API интерфейс
├── DeviceRepository.kt       # Repository для работы с устройством
└── WifiConnectionManager.kt  # Менеджер WiFi подключений
```

## Зависимости

- **AndroidX** - основные компоненты
- **CameraX** - работа с камерой
- **ML Kit Barcode** - распознавание QR кодов
- **Retrofit** - HTTP клиент
- **Gson** - парсинг JSON
- **Coroutines** - асинхронность

## Обработка ошибок

### Неверные данные WiFi
Если пользователь ввёл неверный пароль:
- Устройство не сможет подключиться к сети
- Через 3 попытки вернётся в AP mode
- Пользователь сможет повторить настройку

### Политики безопасности сети
Некоторые корпоративные сети блокируют подключение IoT устройств:
- MAC-фильтрация
- 802.1x аутентификация
- Captive portal

В таких случаях приложение покажет ошибку подключения.

### Устройство недоступно
Если устройство не отвечает:
- Проверьте питание
- Убедитесь что LED мигает (режим AP)
- Удерживайте кнопку сброса 5 сек для возврата к заводским настройкам

## Формат QR кода

```
GREENHOUSE:UID=GH-0001;SSID=Greenhouse-GH-0001;PWD=greenhouse2024
```

Подробнее: см. `nodemcu/QR_CODE_FORMAT.md`

## License

MIT License

